using System;
using System.Collections.Generic;
using System.Linq;

namespace _3._Backpack
{
    class Program 
    {
        static void Main(string[] args) 
        {
            int[] w = { 7, 3, 4, 5 }; // Тегла на предметите в килограми
            int[] v = { 42, 12, 40, 25 }; // Цени на предметите в килограми

            int maxCapacity = 10;  // Максимален капацитет на раницата
            int currCapacity = 0; // Текуша капацитет на раницата
            int maxSum = 0; // Максимална сума
            int currSum = 0; // Текуща сума

            // 1. Вариации на масива W, които се прилагат и на масива V
            // 2. Обхождат се вариациите на W, взима се най-голяма сума от V
            // 3. Извеждаме получената сума
        }


    }
}
