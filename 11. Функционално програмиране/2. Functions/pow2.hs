-- Рекурсивна функция за намиране на степен на двойката
pow2 n =
    if n == 0 
    then 1 -- Дъно на рекурсията 
    else 2 * (pow2 (n - 1)) -- Рекурсия
    
-- Главна функция
main = do

    -- Test: 2^3 = 8
    putStrLn (show(pow2 3))