using CrudWithOrm.Presentation;

/// <summary>
/// Task "CRUD with ORM" namespace.
/// </summary>
namespace CrudWithOrm
{
    /// <summary>
    /// Task "CRUD with ORM" main program class.
    /// </summary>
    public class Program
    {
        /// <summary>
        /// Task "CRUD without ORM" main program method.
        /// </summary>
        public static void Main(string[] args)
        {
            Display display = new Display();
        }
    }
}